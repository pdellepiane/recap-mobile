import type { ImageSourcePropType } from 'react-native';

/** Local asset when event detail has no remote cover for the live-row center avatar. */
export const EVENT_DETAIL_LIVE_ROW_AVATAR_FALLBACK =
  require('../../../../../assets/images/event-detail/avatar-boda-mariel-jorge.png') as ImageSourcePropType;
